# V2 Archive

This directory stores the older V2-era implementation and notes that are no longer part of the active `v3` paper pipeline.

Archived here:

- `geomatric/graph_classify_v2.py`
- the old `geomatric/achivement/` directory
- auxiliary V2 analysis utilities
- V2-stage notes that were used before the stabilized full-suite protocol

The active experiment and paper workflow now lives in:

- `geomatric/graph_classify_v3.py`
- `py/run_paper_experiments.py`
- `py/generate_all_result_reports.py`
- `paper/`
